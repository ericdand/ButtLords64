using UnityEngine;
using System.Collections;

public class CheckpointContainerScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// Expose all child checkpoints to other scripts.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
